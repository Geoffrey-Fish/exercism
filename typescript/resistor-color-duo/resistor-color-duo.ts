export function decodedValue(colors: array): number {
  interface bandColor { color: string; value: number };
  const

  const firstColor = bandColor[colors[0]];
  const secondColor = bandColor[colors[1]];

  const score: string = "";

  score.add(firstColor);
  score.add(secondColor);

  return parseInt(score);
}
'Black': 0,
  'Brown': 1,
    'Red': 2,
      'Orange': 3,
        'Yellow': 4,
          'Green': 5,
            'Blue': 6,
              'Violet': 7,
                'Grey': 8,
                  'White': 9
